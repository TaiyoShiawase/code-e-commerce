<%- include('./partials/head'); %>

<section>
    <% for(var i=0; i < product.length; i++) { %>
        <div class="products">
            <h1><%=product[i].name %></h1>
        </div>
        <% } %>
</section>
    

<%- include('./partials/footer'); %>